# Medium6StockPred
To run it yourself:
pip install yfinance pandas numpy scikit-learn matplotlib seaborn joblib

python stock_classifier.py --train     # first run: trains and saves model
python stock_classifier.py             # subsequent runs: loads saved model
